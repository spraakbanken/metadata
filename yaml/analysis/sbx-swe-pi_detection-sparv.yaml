# This file was automatically generated by Sparv. Do not make changes directly to this file as they will get overwritten.
description:
  swe: "En Sparv-plugin som möjliggör upptäckt och annotering av personlig information
    i svenska texter, särskilt L2 uppsatser (observera att den här annoteringen fungerar
    betydligt sämre för andra domäner, men modellerna som används nu kommer sannolikt
    att uppdateras). Den här modellen garanterar inte att alla personliga uppgifter i texten upptäcks. 
    Använd den aldrig utan översikt av en människa (human-in-the-loop)."
  eng: "A plugin for Sparv for detecting personal information in Swedish texts, especially
    learner essays (note: performs noticeably worse on other domains, but the models
    used for annotation will likely be updated in the future). This model does not guarantee 
    the detection of all personal information in the text. Never use it without human supervision 
    (human-in-the-loop). The model performs noticeably worse on texts that are not student essays."
short_description:
  swe: En Sparv-plugin som möjliggör upptäckt och annotering av personlig information
    i svenska texter, särskilt L2 uppsatser.
  eng: A plugin for Sparv for detecting personal information in Swedish texts, especially
    learner essays.
type: analysis
task: PI detection
analysis_unit: token
models:
  - url: https://huggingface.co/collections/Turtilla/pi-detection-and-labeling-6822fbc8fbccfc2527c019ba
license: MIT license
example: |-
  This analysis is used with Sparv. Check out [Sparv's quick start guide](https://spraakbanken.gu.se/sparv/#/user-manual/quick-start) to get started!

  To use this analysis, add the following line under `export.annotations` in the Sparv [corpus configuration file](https://spraakbanken.gu.se/sparv/#/user-manual/quick-start?id=creating-the-config-file):

  ```yaml
  - <token>:sbx_pi_detection.pi  # None
  ```

  In order to use this plugin you need to add the following setting to your Sparv corpus configuration file with the appropriate argument (basic, basic_iob, general, general_iob, detailed, or detailed_iob):
  ```yaml
  sbx_pi_detection:
    annotation_level: general
  ```

  You also need to install the following plugin: *[sbx_pi_detection](https://github.com/spraakbanken/sparv-sbx-pi-detection)*.

  For general information on how to install plugins, see [here](https://spraakbanken.gu.se/sparv/#/user-manual/installation-and-setup?id=plugins).

  For more info on how to use Sparv, check out the [Sparv documentation](https://spraakbanken.gu.se/sparv).

  Example output:
  ```xml
  <token pi="O">Jag</token>
  <token pi="O">heter</token>
  <token pi="personal_name">Maria</token>
  <token pi="O">.</token>
  ```
contact_info:
  name: Markus Forsberg
  email: sb-info@svenska.gu.se
  affiliation:
    organization: Språkbanken Text
    email: sb-info@svenska.gu.se
